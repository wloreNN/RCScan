# Optional NVD API key for the official NVD API.
# RCScan reads NVD_API_KEY from the process environment; it does not load this
# file automatically. Leave empty for unauthenticated, lower-rate live lookups.
# Never commit a real key; .env is ignored and keys are never cached.
NVD_API_KEY=
